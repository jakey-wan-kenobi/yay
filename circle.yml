test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - ./utils/deploy_prod.sh
  # staging:
  #   branch: staging
  #   commands:
  #     - ./deploy_staging.sh
